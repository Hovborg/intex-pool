<!-- Thanks for contributing! Please check the boxes that apply. -->

## What does this change?



## Checklist

- [ ] `pytest -q` passes
- [ ] `ruff check custom_components tests` is clean
- [ ] If `card/src/` changed: rebuilt the bundle (`cd card && npm run build`) and committed it
- [ ] Bumped the version in **all** of `manifest.json`, `pyproject.toml`, `card/package.json` (kept in sync)
- [ ] Added a `CHANGELOG.md` entry
